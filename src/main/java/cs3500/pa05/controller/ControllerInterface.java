package cs3500.pa05.controller;

import java.io.IOException;

/**
 * Represents a interface for a controller.
 */
public interface ControllerInterface {
  /**
   * Starts the journal program by displaying the password lock screen.
   */
  void run();
}
